#include<stdio.h>
int main()
{
	
	
	   	    
	float logarithm(logarithm_a,base_b)
	
				float x,c,y;
			printf("Please Input the number:\n");
	
					scanf("%f,%f",&x,&y);
	
					c=logarithm(x,y);
				printf("The Cubic is: %.2f\n", c);
		
		
	
return 0;	
	
}



/*Function:Returns the Cubic of a number*/
// function to evaluate logarithm a base-b

float logarithm(logarithm_a,base_b)
{ float result;
	if(logarithm_a===0 || logarithm_a<0) && (base_b==0 ||base_b==1)
		{
		result=	00;
		}
	else 
		result=(log2(a) / log2(b));
    return (result)
}


